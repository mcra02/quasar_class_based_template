{
  "component_name": "Component Example",
  "component_slug": "{{ cookiecutter.component_name.lower()|replace(' ', '-')|replace('.', '-')|trim() }}",
  "component_file_name": "{{ cookiecutter.component_file_name.lower()|map('capitalize')|replace(' ', '')|replace('-', '')|replace('.', '')|trim() }}"
}
